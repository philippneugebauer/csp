<% content_for :title, "New customer success manager" %>

<div class="container py-4">
  <div class="d-flex flex-wrap justify-content-between align-items-center gap-2 mb-3">
    <h1 class="h2 mb-0">New customer success manager</h1>
    <%= link_to customer_success_managers_path, class: "btn btn-outline-secondary" do %>
      <i class="bi bi-arrow-left me-1"></i>Back
    <% end %>
  </div>

  <div class="card shadow-sm">
    <div class="card-body">
      <%= render "form", customer_success_manager: @customer_success_manager %>
    </div>
  </div>
</div>
